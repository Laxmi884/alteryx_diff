---
phase: 10-app-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/server.py
  - app/main.py
  - pyproject.toml
  - tests/conftest.py
  - tests/test_port_probe.py
  - tests/test_server.py
autonomous: true
requirements:
  - APP-01
  - APP-03
  - APP-04

must_haves:
  truths:
    - "FastAPI server starts on port 7433 (or next available 7434-7443) and stays running"
    - "GET /health returns {status: 'ok', version: 'x.x.x'} with 200"
    - "Port probe returns the first available port and a pre-bound socket — no race condition"
    - "Port probe raises OSError when all ports 7433-7443 are occupied"
    - "A browser tab opens at the correct localhost:PORT URL one second after server start"
    - "app/main.py calls multiprocessing.freeze_support() to prevent infinite spawning on Windows"
  artifacts:
    - path: "app/server.py"
      provides: "FastAPI app with /health route and StaticFiles mount"
      exports: ["app"]
      contains: "sys._MEIPASS"
    - path: "app/main.py"
      provides: "Entry point: port probe, uvicorn start, browser open"
      contains: "freeze_support"
    - path: "tests/test_port_probe.py"
      provides: "Unit tests for find_available_port()"
    - path: "tests/test_server.py"
      provides: "Unit tests for /health endpoint and SPA fallback"
  key_links:
    - from: "app/main.py"
      to: "app/server.py"
      via: "from app.server import app as fastapi_app"
      pattern: "from app\\.server import app"
    - from: "app/main.py"
      to: "uvicorn.Server"
      via: "asyncio.run(server.serve(sockets=[sock]))"
      pattern: "serve\\(sockets"
    - from: "app/server.py"
      to: "app/frontend/dist"
      via: "StaticFiles(directory=_static_dir(), html=True)"
      pattern: "StaticFiles.*html=True"
---

<objective>
Build the FastAPI backend skeleton and Python entry point for the Alteryx Git Companion.

Purpose: Provides the server and startup logic the .exe will run — port probe, uvicorn programmatic start, browser open, /health endpoint, and StaticFiles mount for the React frontend. All logic is unit-tested before packaging.

Output: app/server.py, app/main.py, updated pyproject.toml with fastapi/uvicorn deps, and passing unit tests for port probe and /health.
</objective>

<execution_context>
@/Users/laxmikantmukkawar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laxmikantmukkawar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-app-scaffold/10-CONTEXT.md
@.planning/phases/10-app-scaffold/10-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test scaffold for port probe and server health</name>
  <files>tests/conftest.py, tests/test_port_probe.py, tests/test_server.py</files>
  <behavior>
    - test_find_available_port_returns_7433: when 7433 is free, find_available_port() returns (7433, socket)
    - test_find_available_port_skips_occupied: when 7433 is bound by another socket, returns next free port (7434)
    - test_find_available_port_raises_when_all_full: when all 7433–7443 are bound, raises OSError
    - test_health_returns_200: GET /health returns 200 with JSON body containing "status" and "version" keys
    - test_health_status_ok: response body has status == "ok"
    - test_spa_fallback: GET /nonexistent-route returns 200 with index.html content (StaticFiles html=True fallback) — mock the dist dir with a minimal index.html
  </behavior>
  <action>
    Create tests/conftest.py with a FastAPI TestClient fixture. The TestClient must patch _static_dir() to point at a temporary directory containing a minimal index.html — this avoids needing the real frontend build during tests.

    Create tests/test_port_probe.py testing find_available_port() from app.main. Use socket fixtures that pre-bind ports to simulate occupied/free states.

    Create tests/test_server.py testing the FastAPI app. Import `app` from app.server; wrap in TestClient. For SPA fallback test, monkeypatch app.server._static_dir to return a tmp_path containing index.html with content "<!DOCTYPE html><html></html>".

    These tests MUST FAIL (RED) before Tasks 2 and 3 exist. Mark them with a comment: "# RED: app.main and app.server do not exist yet".
    Run: uv run pytest tests/test_port_probe.py tests/test_server.py -x -q
    Expected: ImportError or ModuleNotFoundError — confirms RED state.
  </action>
  <verify>
    <automated>uv run pytest tests/test_port_probe.py tests/test_server.py -x -q 2>&1 | grep -E "(ERROR|ImportError|ModuleNotFoundError|FAILED)" | head -5</automated>
  </verify>
  <done>Test files exist with the 6 test cases listed above; running pytest shows import errors (RED state confirmed — app.main and app.server don't exist yet)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement FastAPI server and entry point</name>
  <files>app/__init__.py, app/server.py, app/main.py, pyproject.toml</files>
  <behavior>
    - find_available_port(7433, 11) returns (7433, socket.socket) when 7433 is free
    - find_available_port skips occupied ports and returns next available
    - find_available_port raises OSError when range 7433–7443 all occupied
    - GET /health returns {"status": "ok", "version": "..."}
    - StaticFiles mounted AFTER /health route (catch-all must be last)
    - _static_dir() returns sys._MEIPASS / "frontend" / "dist" when frozen, else Path(__file__).parent / "frontend" / "dist"
    - main() calls multiprocessing.freeze_support() as first statement
    - main() schedules webbrowser.open via threading.Timer(1.0, ...) so it fires after server is listening
    - server is started via asyncio.run(server.serve(sockets=[sock])) with pre-bound socket (no race condition)
  </behavior>
  <action>
    Create app/__init__.py (empty).

    Create app/server.py:
    - Import FastAPI, StaticFiles, sys, Path
    - Define _static_dir() function: if getattr(sys, "frozen", False) use Path(sys._MEIPASS) / "frontend" / "dist", else use Path(__file__).parent / "frontend" / "dist"
    - Create FastAPI() instance as `app`
    - Define @app.get("/health") returning {"status": "ok", "version": version("alteryx-diff")} using importlib.metadata.version
    - Mount StaticFiles LAST: app.mount("/", StaticFiles(directory=str(_static_dir()), html=True), name="frontend") — wrapped in try/except so missing dist/ dir doesn't crash the server during dev/test (log a warning instead)

    Create app/main.py:
    - import multiprocessing, socket, asyncio, webbrowser, threading, uvicorn
    - from app.server import app as fastapi_app
    - find_available_port(start=7433, count=11): loop range(start, start+count), attempt sock.bind(("127.0.0.1", port)), return (port, sock) on first success, raise OSError if all fail
    - sock.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1) before bind
    - main(): multiprocessing.freeze_support() FIRST; call find_available_port(); print startup log with actual port; schedule webbrowser.open via threading.Timer(1.0, lambda: webbrowser.open(f"http://localhost:{port}")); build uvicorn.Config(fastapi_app, host="127.0.0.1", port=port, log_level="warning"); run asyncio.run(server.serve(sockets=[sock]))
    - if __name__ == "__main__": main()

    Update pyproject.toml dependencies to add: "fastapi>=0.111", "uvicorn>=0.29", "pyinstaller>=6.6", "pyinstaller-versionfile>=3.0"
    Run: uv add fastapi uvicorn pyinstaller pyinstaller-versionfile

    After implementing, run tests to reach GREEN:
    uv run pytest tests/test_port_probe.py tests/test_server.py -x -q
  </action>
  <verify>
    <automated>uv run pytest tests/test_port_probe.py tests/test_server.py -x -q</automated>
  </verify>
  <done>All 6 tests pass (GREEN). app/server.py and app/main.py exist. pyproject.toml lists fastapi, uvicorn, pyinstaller, pyinstaller-versionfile in dependencies.</done>
</task>

</tasks>

<verification>
Full backend scaffold verified:
- uv run pytest tests/test_port_probe.py tests/test_server.py -v
- uv run python -c "from app.server import app; print('server import OK')"
- uv run python -c "from app.main import find_available_port; p, s = find_available_port(); s.close(); print(f'port probe OK: {p}')"
</verification>

<success_criteria>
- All 6 unit tests pass
- app/server.py exports `app` (FastAPI instance) with /health route defined before StaticFiles mount
- app/main.py exports find_available_port() and main() with freeze_support() call
- pyproject.toml has fastapi, uvicorn, pyinstaller, pyinstaller-versionfile as dependencies
- _static_dir() correctly branches on sys.frozen
</success_criteria>

<output>
After completion, create `.planning/phases/10-app-scaffold/10-01-SUMMARY.md`
</output>
